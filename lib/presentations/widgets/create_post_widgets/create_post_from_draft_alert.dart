import 'package:flutter/material.dart';

class CreatePostFromDraft extends StatelessWidget {
  const CreatePostFromDraft({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}