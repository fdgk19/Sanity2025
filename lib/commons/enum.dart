enum Options {
  home,
  notification,
  profile,
  subscriptions,
  settings,
  assistance,
  research,
  logout,
}

enum Setting {
  preferences,
  security,
  privacy,
  code,
  subscription,
  assistance,
}

enum PlanType { free, pro, angel }

enum InputCreateType { none, text, video, audio, image, url, signature }
